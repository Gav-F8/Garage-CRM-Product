{
  "name": "garage-crm-functions",
  "description": "Cloud Functions for Classic Garage Management — sets Firebase Auth custom claims (role + businessId) used by Firestore security rules and the client AuthContext.",
  "type": "module",
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "lint": "echo \"(no linter configured for functions)\"",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "backfill-claims": "node scripts/backfillClaims.js"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.1.0"
  }
}
